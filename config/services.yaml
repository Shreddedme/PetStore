# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    pet_param_converter:
        class: App\ParamConverter\PetParamConverter
        tags:
            - { name: request.param_converter, converter: pet_param_converter }

    user_param_converter:
        class: App\ParamConverter\UserParamConverter
        tags:
            - { name: request.param_converter, converter: user_param_converter }

#    Symfony\Component\Serializer\Normalizer\DateTimeNormalizer: ~
#
#    App\Normalizer\PetNormalizer:
#        class: App\Normalizer\PetNormalizer
#        tags:
#            - { name: serializer.normalizer }
#
#    App\Normalizer\UserNormalizer:
#        class: App\Normalizer\UserNormalizer
#        tags:
#            - { name: serializer.normalizer }
#
#    Symfony\Component\Serializer\Encoder\JsonEncoder: ~
#
#    Symfony\Component\Serializer\Serializer:
#        arguments:
#            - [ '@App\Entity\Normalizer\PetNormalizer', '@Symfony\Component\Serializer\Normalizer\DateTimeNormalizer' ]
#            - [ '@Symfony\Component\Serializer\Encoder\JsonEncoder' ]


    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    App\EventListener\ValidationExceptionListener:
        tags:
            - { name: kernel.event_listener, event: kernel.exception, priority: 1}


    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones
