<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@1/css/pico.min.css" />
</head>
<main class="container">
    <section id="tables">
        <h2>Список питомцев</h2>
        <figure>
            <table role="grid">
                <thead>
                <tr>
                    <th><a href="{{ path('app_pet_list', {'sort_by': 'id', 'sort_direction': (sortBy == 'id' and sortDirection == 'asc') ? 'desc' : 'asc'}) }}">ID</a></th>
                    <th><a href="{{ path('app_pet_list', {'sort_by': 'name', 'sort_direction': (sortBy == 'name' and sortDirection == 'asc') ? 'desc' : 'asc'}) }}">Имя</a></th>
                    <th><a href="{{ path('app_pet_list', {'sort_by': 'description', 'sort_direction': (sortBy == 'description' and sortDirection == 'asc') ? 'desc' : 'asc'}) }}">Описание</a></th>
                    <th><a href="{{ path('app_pet_list', {'sort_by': 'owner', 'sort_direction': (sortBy == 'owner' and sortDirection == 'asc') ? 'desc' : 'asc'}) }}">Владелец</a></th>
                    <th>Действия</th>
                </tr>
                </thead>
                <tbody>
                {% for pet in pets %}
                    <tr>
                        <td>{{ pet.id }}</td>
                        <td>{{ pet.name }}</td>
                        <td>{{ pet.description }}</td>
                        <td>{{ pet.owner.name }}</td>
                        <td>
                            <a href="{{ path('app_pet_update', {'id': pet.id}) }}">Обновить</a>&nbsp;&nbsp;&nbsp;
                            <a href="{{ path('app_pet_delete', {'id': pet.id}) }}">Удалить</a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </figure>
    </section>
</main>
</html>
